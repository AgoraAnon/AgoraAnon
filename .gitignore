# Claude Code harness project directory (local-only)
.claude/

# Local / editor noise
.DS_Store
Thumbs.db
*~
*.swp
*.swo

# Environment / secrets — never commit
.env
.env.*
*.pem
*.key

# Local-only AI notes (machine paths, host facts) — never pushed
AINOTES.local.md
